// import test, { expect } from "playwright/test";

// test("should display the pet name", async ({ page }) => {
//   // Navigate to the PetProfile page (Make sure the route is correct for your app)
//   await page.goto("http://localhost:5174/profile/1"); // Adjust to your page's route

//   // Check if the pet name (label or any element) is displayed correctly
//   const petNameLocator = page.locator(
//     "h2.text-4xl.font-bold.text-gray-900.mb-3"
//   ); // Assuming the pet name is inside an <h2> element

//   // Assert that the pet name is as expected
//   expect(petNameLocator).toBe("Fluffy"); // Replace 'Fluffy' with the expected value
// });
